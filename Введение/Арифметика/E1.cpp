#include <bits/stdc++.h>
using namespace std;

int main(){
	int n;
	cin >> n;
	printf("The next number for the number %d is %d.\n", n, n+1);
	printf("The previous number for the number %d is %d.", n, n-1);
	return 0;
}
